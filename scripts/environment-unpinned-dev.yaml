channels:
    - conda-forge
dependencies:
    - autopep8
    - flake8
    - flake8-isort
    - ipython
    - localstack==0.11.1.1
    - pip
    - pre-commit
    - pytest
    - pip:
        # Select depenedencies from localstack[full] that we need
        # These should be pinned to compatible versions for the current
        # localstack version
        - amazon-kclpy==2.0.1
        - crontab==0.22.6
        - flask-cors==3.0.8
        - moto-ext==1.3.15.15
        - subprocess32==3.5.4
