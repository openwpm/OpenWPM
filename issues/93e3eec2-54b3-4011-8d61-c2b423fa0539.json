{
  "uuid": "93e3eec2-54b3-4011-8d61-c2b423fa0539",
  "display_id": 58,
  "title": "Fix geckodriver SIGABRT on teardown (FIFO->regular file) [GH #1208]",
  "description": "Steps to reproduce:\n1. \n\nExpected: \nActual: \n\nGitHub #1208: geckodriver killed with SIGABRT (broken-pipe panic, exit 134 + coredump) on every browser teardown/recycle (bursts of N = parallel browsers). Root cause: OpenWPM routes geckodriver stdout through a FIFO (FirefoxLogInterceptor) whose daemon-thread reader can close the read end before geckodriver stops writing -> EPIPE -> Rust double-panic-in-destructor -> abort(). Harmless to crawl data but spams coredumps + muddies exit bookkeeping. FIX (per the issue): back the interceptor with a REGULAR FILE instead of a FIFO (writes never EPIPE); interceptor tails the file + stops on request. Files: openwpm/deploy_browsers/selenium_firefox.py (FirefoxLogInterceptor) + deploy_firefox.py (log_output). VERIFY via a DIRECT-selenium repro (multi-browser crawl + recycle -> confirm no exit-134/coredumps); the TaskManager multiprocess path is broken under Python 3.14 separately. Full analysis + suggested diff in GH #1208.",
  "status": "open",
  "priority": "high",
  "created_by": "claude",
  "created_at": "2026-06-20T23:50:49.233602801Z",
  "updated_at": "2026-06-21T01:25:25.270455735Z",
  "labels": [
    "bug",
    "pumpable"
  ],
  "comments": [
    {
      "uuid": "b5e4cd0d-a735-4757-ae96-1a9e4168f26c",
      "author": "claude",
      "content": "Landed as DRAFT PR #1209 (https://github.com/openwpm/OpenWPM/pull/1209), branch fix/geckodriver-fifo-sigabrt, base master.\n\nFix: backed FirefoxLogInterceptor with a REGULAR FILE instead of a FIFO. Regular-file writes never raise EPIPE, so geckodriver can no longer be aborted by reader/teardown ordering — removes the SIGABRT-on-teardown failure mode entirely (not just narrowing the race).\n\nChanges (3 files):\n- selenium_firefox.py: tempfile.mkstemp instead of mktempfifo; interceptor tails the file and stops on an explicit threading.Event (stop()), draining remaining lines then unlinking; removed unused mktempfifo helper. daemon flag retained.\n- deploy_firefox.py: log_output -> regular file; return interceptor to caller.\n- browser_manager.py: run_impl finally-block calls webdriver_interceptor.stop() (after driver already quit/killed, so no write can race).\n\nVerification:\n- Deterministic OS-level proof: FIFO write after reader-close -> EPIPE; regular-file write after reader-close -> succeeds (root cause confirmed + fix removes it).\n- Session-driven direct-Selenium repro (geckodriver 0.37.0 / FF 152): regular-file interceptor -> geckodriver survives reader teardown mid-session, log lines captured, clean exit. The exact FIFO abort window is timing-finicky to trigger on demand; OS-level proof + structural argument cover correctness, full validation on CI.\n- pytest -m pyonly green; mypy clean on both deploy files; black/isort clean; pre-commit green.\n\nNote: deeper cause is upstream (geckodriver/Rust should treat broken-pipe-on-stdout as clean exit, not panic-to-abort) — flagged in PR for a separate Mozilla report. Closes #1208. Not merged; draft for human review.",
      "created_at": "2026-06-21T00:30:02.521706452Z",
      "kind": "note"
    },
    {
      "uuid": "4188ec83-4b85-4da6-884d-4e097f7213e7",
      "author": "claude",
      "content": "HATER REVIEW (read-only, draft #1209) — VERDICT: Acceptable. Crash fix is SOUND: regular file removes the EPIPE mechanism (not just the window); verified at OS level; both teardown paths (graceful os._exit + forced SIGKILL) covered; no surviving FIFO/stderr EPIPE path. BUT two real defects gate merge: (1 MAJOR) temp-file LEAK on every teardown — unlink now lives only in run()'s finally, which never runs under os._exit() (daemon thread, no join) or SIGKILL. Original unlinked on first log line, eagerly+unconditionally. Long crawls leak thousands of owpm_driver_*.log in /tmp. Fix: unlink-on-open (after writer opens line 155); anonymous inode carried by open fds, leak-proof on all paths, deletes the drain dance. (2 MAJOR) stop()+drain is unjoined dead code — nothing waits for the drain before process exit. Plus (MINOR) regular-file readline() fragments lines split across flushes/polls into 2 log lines (debug-fidelity regression); (MINOR) zero test coverage for FirefoxLogInterceptor. Recommend: adopt unlink-on-open, delete stop()/drain, add 1 pyonly test. Full writeup: /home/vringar/projects/OpenWPM/datadir/hater-1209.md",
      "created_at": "2026-06-21T00:55:04.278545364Z",
      "kind": "note"
    },
    {
      "uuid": "44b375ee-0ede-49a8-a7a6-c2eabff0aa90",
      "author": "claude",
      "content": "Applied the-hater's fixes to PR #1209 (force-updated draft).\n\n**Issue #1 + #2 (Majors) collapsed via unlink-on-open:**\n- `selenium_firefox.py`: `FirefoxLogInterceptor` now opens the read fd in `start()` (before the writer opens by path). Removed the `threading.Event`/`stop()`/drain machinery entirely (it was unjoined dead code — daemon thread, no `join()`, would never run before `os._exit()`).\n- `deploy_firefox.py`: keep a reference to the geckodriver log write handle, then `os.unlink()` the path immediately after opening it. Both fds carry the now-anonymous inode to process exit. Leak-proof on all paths (graceful os._exit(), SIGKILL, exception) with no teardown hook. Verified no `/tmp/owpm_driver_*.log` leak.\n- `browser_manager.py`: dropped the `webdriver_interceptor.stop()` call in `run_impl` finally; interceptor is now a self-cleaning daemon.\n\n**Issue #3 (Minor, line fragmentation):** fixed — the tail loop buffers partial `readline()` results until a newline, so a driver line split across flushes/polls is emitted as one log entry (not two).\n\n**Issue #4 (Minor, zero coverage):** added `test/test_selenium_firefox.py` (3 pyonly tests): lines reach the logger; temp file unlinked-but-readable-via-fd (no leak); split writes not fragmented.\n\n**Issue #5 (Nitpick):** addressed incidentally — the write handle now has an explicit reference (`driver_log_fh`).\n\n**Crash fix intact:** regular file, no surviving FIFO codepath.\n\n**Verification:** 3/3 pyonly tests green; mypy clean on the 3 touched files; black/isort clean; jj-precommit green. Commit message updated to describe the unlink-on-open approach (was stale re: the removed Event/drain).",
      "created_at": "2026-06-21T01:25:25.267623104Z",
      "kind": "note"
    }
  ]
}